import styled from '@emotion/styled';
export const MainWrapper = styled.div((props: any) => {
  return {
    height: '100%',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  };
});
